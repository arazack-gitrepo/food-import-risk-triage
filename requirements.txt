# Core pipeline
pandas>=2.0
numpy>=1.24
scikit-learn>=1.4
joblib>=1.3
matplotlib>=3.7
tabulate>=0.9

# Dashboard
streamlit>=1.40

# Optional comparison model. The pipeline runs without it.
# lightgbm>=4.5

# Optional: DistilBERT arm, run separately on GPU (see scripts/).
# torch>=2.0
# transformers>=4.40
